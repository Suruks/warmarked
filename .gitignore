# Godot 4+ specific ignores
.godot/
/android/

# Экспортные сборки и временные файлы
/build/
/tests/_capture.png

# Виндовые lock/temp-артефакты (антивирус и т.п.), которые --import иногда оставляет
# рядом с бинарниками аддонов при пересборке - не файлы проекта, попадают под git add -A.
**/~*
